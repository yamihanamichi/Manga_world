<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - Manga World</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="/css/style.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Lexend+Giga&display=swap" rel="stylesheet">
</head>
<body>
    <header class="navbar navbar-expand-lg navbar-dark manga-header">
        <div class="container">
            <a class="navbar-brand" href="/">
                <img src="/images/logo4.png" alt="Manga World Logo" class="logo">
            </a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <nav class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item">
                        <a class="nav-link <%= page === 'home' ? 'active' : '' %>" href="/"><i class="fas fa-home"></i> Accueil</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link <%= page === 'classement' ? 'active' : '' %>" href="/classement"><i class="fas fa-trophy"></i> Classement</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link <%= page === 'maliste' ? 'active' : '' %>" href="/maliste"><i class="fas fa-list"></i> Ma Liste</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link <%= page === 'moncompte' ? 'active' : '' %>" href="/moncompte"><i class="fas fa-user"></i> Mon Compte</a>
                    </li>
                    <% if (locals.user && user.role === 'admin') { %>
                        <li class="nav-item">
                            <a class="nav-link <%= page === 'admin' ? 'active' : '' %>" href="/admin">
                                <i class="fas fa-shield-alt"></i> Admin
                            </a>
                        </li>
                    <% } %>
                </ul>
            </nav>
        </div>
    </header>